/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AuditEventResponse } from '../../models/audit-event-response';

export interface EventosIdGet$Params {
  id: string;
  'X-Correlation-Id'?: string;
}

export function eventosIdGet(http: HttpClient, rootUrl: string, params: EventosIdGet$Params, context?: HttpContext): Observable<StrictHttpResponse<AuditEventResponse>> {
  const rb = new RequestBuilder(rootUrl, eventosIdGet.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
    rb.header('X-Correlation-Id', params['X-Correlation-Id'], {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<AuditEventResponse>;
    })
  );
}

eventosIdGet.PATH = '/eventos/{id}';
