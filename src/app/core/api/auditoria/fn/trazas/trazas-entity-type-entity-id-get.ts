/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { EntityTraceResponse } from '../../models/entity-trace-response';
import { EntityTypeEnum } from '../../models/entity-type-enum';

export interface TrazasEntityTypeEntityIdGet$Params {

/**
 * Identificador de trazabilidad distribuida.
 */
  'X-Correlation-Id'?: string;
  entityType: EntityTypeEnum;
  entityId: string;
  from?: string;
  to?: string;

/**
 * Si es true, incluye campos hash/prevHash para verificación de integridad.
 */
  includeIntegrity?: boolean;
}

export function trazasEntityTypeEntityIdGet(http: HttpClient, rootUrl: string, params: TrazasEntityTypeEntityIdGet$Params, context?: HttpContext): Observable<StrictHttpResponse<EntityTraceResponse>> {
  const rb = new RequestBuilder(rootUrl, trazasEntityTypeEntityIdGet.PATH, 'get');
  if (params) {
    rb.header('X-Correlation-Id', params['X-Correlation-Id'], {});
    rb.path('entityType', params.entityType, {});
    rb.path('entityId', params.entityId, {});
    rb.query('from', params.from, {});
    rb.query('to', params.to, {});
    rb.query('includeIntegrity', params.includeIntegrity, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<EntityTraceResponse>;
    })
  );
}

trazasEntityTypeEntityIdGet.PATH = '/trazas/{entityType}/{entityId}';
