/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RegistroCiudadanoRequest } from '../../models/registro-ciudadano-request';
import { RegistroUsuarioResponse } from '../../models/registro-usuario-response';

export interface RegisterCitizen$Params {

/**
 * Identificador de trazabilidad distribuida.
 */
  'X-Correlation-Id'?: string;
      body: RegistroCiudadanoRequest
}

export function registerCitizen(http: HttpClient, rootUrl: string, params: RegisterCitizen$Params, context?: HttpContext): Observable<StrictHttpResponse<RegistroUsuarioResponse>> {
  const rb = new RequestBuilder(rootUrl, registerCitizen.PATH, 'post');
  if (params) {
    rb.header('X-Correlation-Id', params['X-Correlation-Id'], {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<RegistroUsuarioResponse>;
    })
  );
}

registerCitizen.PATH = '/register/citizen';
