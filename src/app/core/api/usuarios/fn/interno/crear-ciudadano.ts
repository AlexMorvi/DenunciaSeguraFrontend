/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RegistroCiudadanoRequest } from '../../models/registro-ciudadano-request';
import { UsuarioResponse } from '../../models/usuario-response';

export interface CrearCiudadano$Params {
      body: RegistroCiudadanoRequest
}

export function crearCiudadano(http: HttpClient, rootUrl: string, params: CrearCiudadano$Params, context?: HttpContext): Observable<StrictHttpResponse<UsuarioResponse>> {
  const rb = new RequestBuilder(rootUrl, crearCiudadano.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<UsuarioResponse>;
    })
  );
}

crearCiudadano.PATH = '/interno/usuarios/ciudadano';
